---
import BaseLayout from "./BaseLayout.astro";

interface Props {
  prevNav?: string;
  title: string;
  description: string;
}

const { prevNav, title, description } = Astro.props;
---

<BaseLayout title={title} description={description}>
  <slot />
</BaseLayout>
